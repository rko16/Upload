@extends('app')
@section('content')
<div class="wrap bg-light">
    <div class="container mt-5 pt-2">
        <div class="form-box">
            <div class="row justify-content-center">
                <div class="col-12 col-sm-8 col-md-6 col-lg-6">
                     <div class="login-white-box bg-white">
                        <div class="title">
                            <h3>Verification Code</h3>
                            <p>Enter the verification code sent on your register mobile number</p>
                        </div>
                        
                        <form action="{{ route('otp.verify') }}" method="POST">
                            @csrf
                            <div class="form-group">
                                <label for="otp">Enter OTP:</label>
                                <input type="text" id="otp" name="otp" class="form-ctrl form-control" required>
                            </div>
                            @if ($message = Session::get('success'))
                            <div class="alert alert-success alert-block">
                                <button type="button" class="close" data-dismiss="alert">×</button>    
                                <strong>{{ $message }}</strong>
                            </div>
                            @endif
                            @if ($message = Session::get('error'))
                            <div class="alert alert-danger alert-block">
                                <button type="button" class="close" data-dismiss="alert">×</button>    
                                <strong>{{ $message }}</strong>
                            </div>
                            @endif
                            <button type="submit" class="btn btn-warning btn-lg btn-submit btn-block">Verify OTP</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@endsection